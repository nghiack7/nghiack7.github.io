import type { Express } from 'express';
import type { Server } from 'node:http';
import { WebSocketServer } from 'ws';
export interface ServerInstance {
    app: Express;
    server: Server;
    wss: WebSocketServer;
    port: number;
    listenAddress: string;
    publicAddress: string;
    verbose: boolean;
    isMock: boolean;
}
export declare function startServer(port: number, listenAddress: string, publicAddress: string, verbose?: boolean, isMock?: boolean): Promise<ServerInstance>;
export declare function stopServer(serverInstance: ServerInstance): Promise<void>;
